@AGENTS.md

# Claude Notes

Use `AGENTS.md` as the source of truth for coding rules and `DESIGN.md` for product/design direction.

Before editing framework-specific Next.js behavior, read the matching local documentation under `node_modules/next/dist/docs/`.
